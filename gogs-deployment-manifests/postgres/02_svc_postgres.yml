---
apiVersion: v1
kind: Service
metadata:
  name: hostname-postgres
  labels:
    app: workshop-postgres
  #namespace: gogs-test  
spec:
  ports:
  - port: 5432
    protocol: TCP
  selector:
    app: workshop-postgres
...  